<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    
      <div class="name">
        <%= f.text_field :name, placeholder: "Имя", class: "form-control" %></br>
      </div>
    
      <div class="phone">
        <%= f.text_field :phone, placeholder: "Номер телефона", class: "form-control" %></br>
      </div>
  </div>


  <div class="Send">
    <%= f.submit "Отправить", class: "btn btn-default btn-primary" %>
  </div>
<% end %>
